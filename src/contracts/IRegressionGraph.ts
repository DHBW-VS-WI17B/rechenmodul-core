/**
 * A regression graph.
 */
export interface IRegressionGraph {
  /**
   * Y-axis section.
   */
  yAxisSection: number;
  /**
   * Incline of the regression graph.
   */
  incline: number;
  /**
   * Quality of the regression graph.
   */
  quality: number;
}
